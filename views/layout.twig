<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="UTF-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		<title>Simple Mock API</title>

		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.0/css/bootstrap.min.css" crossorigin="anonymous"/>
		<style>
			.log-detail td, .log-detail th {
				padding: .25rem;
				font-size: .85rem;
			}
		</style>
	</head>

	<body>
		<div class="container">
			<div class="row">
				<div class="col-lg-6 offset-lg-3 text-center">
					<form action="/set-user">
						<h3>Your User ID</h3>
						<p class="text-center">Save this ID to use on other devices</p>
						<div class="input-group">
							<input type="text" class="form-control" name="userId" value="{{ userId }}" placeholder="User ID here" required/>
							<div class="input-group-append">
								<button class="btn btn-primary">Set ID</button>
							</div>
						</div>
					</form>
				</div>
			</div>
		</div>
		<hr>
		<div class="container-fluid">
			<div class="row">
				<div class="col-12 col-lg-3 mb-4">
					<div class="card" style="padding: 15px">
						<div class="">
							<a href="/requests/create" class="btn btn-success btn-block">Create new request</a>
						</div>
						<hr>
						{% block left %}
						<h5 class="text-center">Your requests</h5>
						<div class="list-group">
							{% for request in userRequests %}
								<a href="/requests/{{request.id}}/edit" class="list-group-item list-group-item-action {{ " active" if requestData.id == request.id else " " }}">{{request.name | default(request.id)}}</a>
							{% endfor %}
						</div>
						{% endblock %}
					</div>
				</div>
				<div class="col-12 col-lg-9">
					<div class="tab-content">
						<div class="tab-pane active show">
							{% block content %}
								&nbsp;
							{% endblock %}
						</div>
					</div>
				</div>
			</div>
		</div>
	</body>
</html>
